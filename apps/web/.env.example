# Next.js — NEXT_PUBLIC_ prefix makes vars available in the browser bundle.
# Never put secrets here.

# URL of the Express API — used by server components and client fetches
NEXT_PUBLIC_API_URL=http://localhost:3001
